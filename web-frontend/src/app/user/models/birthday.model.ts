export class Birthday {
  private month: string;
  private day: string;
  private year: string;

  constructor(month: string, day: string, year: string) {
    this.month = month;
    this.day = day;
    this.year = year;
  }
}
